<%= form_for(@college_class) do |f| %>
  <% if @college_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@college_class.errors.count, "error") %> prohibited this college_class from being saved:</h2>

      <ul>
      <% @college_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.text_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
